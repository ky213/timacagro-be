scalar DateTime

type Entity {
  id: ID!
  createdAt: DateTime
  updatedAt: DateTime
}

interface Pagination {
  page: Int!
  perPage: Int!
  total: Int!
}

enum Role {
  ADMIN
  ATC
  SALES
  COMMERCE
}

enum Region {
  CENTER
  CENTER_EAST
  NORTH_WEST
  FAR_WEST
  NORTH_EAST
  SOUTH_EAST
  FAR_EAST
  DEV_ZONE_SOUTH
}

type Query {
  getDateTime: DateTime!
}

type Mutation {
  randomize: Float!
}

type Subscription {
  testConnection: Int!
  randomNumber: Float!
}
